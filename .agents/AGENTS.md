# Agent-to-Agent (A2A) Workflow & Rules

The A2A (Agent-to-Agent) architecture separates roles into **Planner** (business analysis), **Architecture** (system design), and **Moderator** (coordination, evaluation, and planning) to minimize hallucinations and increase the practicality of the final plan.

## 1. Communication Flow (A2A Workflow)

1. **User** sends the initial request (e.g., "Create a video sharing app") to the **Moderator**.
2. **Moderator** identifies this as the requirements-gathering phase and forwards it to the **Planner**.
3. **Planner** acts autonomously. It immediately analyzes the request, makes logical assumptions for any missing details, and generates a Business Requirement Document (BRD) without asking the user questions.
4. **Planner** sends the finalized business requirements summary back to the **Moderator**.
5. **Moderator** forwards these requirements to **Architecture** for system design (Tech stack, Database, Microservices/Monolithic, Infrastructure).
6. **Architecture** returns the technical blueprint to the **Moderator**.
7. **Moderator** conducts a Review: Compares the architecture against requirements, estimates costs (AWS, API...), and time (man-months). If unreasonable, requests Architecture to revise.
8. Once finalized, the **Moderator** synthesizes everything and outputs the `Implementation_Plan.md` file.

## 2. Output Template (`Implementation_Plan.md`)

The Moderator is responsible for filling data into this structure after the A2A loop concludes:

```markdown
# 🚀 Implementation Plan: [Project Name]

## 1. Requirements Analysis (Planner Summary)
* **Core Objectives:** ...
* **Target Platforms:** ...
* **Key Features:** ...
* **Target Audience:** ...

## 2. Architecture Blueprint
* **System Model:** (e.g., Microservices with API Gateway)
* **Architecture Diagram:**
  ```mermaid
  graph TD;
      A-->B;
  ```
* **Tech Stack:**
  * **Frontend:** ...
  * **Backend:** ...
  * **DevOps/Infra:** ... (e.g., Dockerized, CI/CD pipelines)
* **Database & Data Storage:** (Where data is stored and how it's organized)
* **Service Communication:** (e.g., REST, gRPC, Message Queues if distributed)
* **Core Processing Flows:** ... (Explain how data moves through the system)
* **Query Optimization:** (Caching strategies, indexing, speed optimization)

## 3. Evaluation & Risks (Moderator Evaluation)
* **Architectural Strengths:** ...
* **Potential Risks (Bottlenecks):** ...
* **Risk Mitigation Strategies:** ...

## 4. Resource Estimation
* **Implementation Timeline:** X weeks (Divided into Sprints: Sprint 1 - Setup, Sprint 2 - Core...)
* **Estimated Infrastructure Costs:** $X/month (List: Server, CDN, Storage, Database)

---
*Generated by the AI Architect Agent Team*
```

## 3. JSON Export Schema

When the plan is finalized, the system will export a detailed JSON file containing the full context of the session. The output MUST conform to the following schema:

```json
{
  "project_status": "Approved",
  "additional_requirements": "Any extra knowledge, notes, or features requested by the user for the next phase",
  "implementation_plan": "The full Markdown text of the finalized implementation plan",
  "conversation_history": [
    {
      "role": "user | moderator | planner | architecture",
      "content": "Message content"
    }
  ]
}
```

## 4. Automation Rules

To best support the development process, agents must adhere to the following automation rules:

1. **Auto-Fixing/Self-Healing:** 
   - During the Implementation Phase, if the system detects an error (compile error, linter warning, test failed, or runtime error), the Agent is responsible for automatically analyzing the error logs.
   - The Agent must automatically propose a solution and fix the code directly without waiting for additional instructions from the user, unless the error relates to a complex system design or requires changing the initial requirements.

2. **Auto Commit & Push (Component-based):**
   - Cần chia nhỏ các commit theo từng phần thay đổi độc lập (Component-based commits). Thay vì commit toàn bộ mọi thứ cùng lúc, Agent phải commit riêng rẽ cho từng tính năng, từng file hoặc từng phần logic (VD: frontend riêng, database schema riêng).
   - Bắt buộc tuân thủ tiền tố phân loại commit (Commit Types):
     * `feat`: thêm chức năng mới.
     * `update`: cải thiện chức năng hiện có.
     * `delete`: xóa bỏ chức năng cũ hoặc file thừa.
     * `maintain`: bảo trì code, tái cấu trúc (refactor) và sửa lỗi.
   - Commit message phải bao gồm chữ ký danh tính (authored by AI Agent). VD: `update(architecture): refine commit types - by Antigravity Agent`.
   - Ngay sau đó, tự động thực hiện các lệnh Git để push code lên nhánh hiện hành trên kho lưu trữ từ xa, đảm bảo tiến độ luôn đồng bộ.

3. **Chạy liên tục cho đến khi đạt mục tiêu (Run until goal is met):**
   - Sau khi thực hiện bất kỳ chỉnh sửa code nào, Agent không được dừng lại giữa chừng mà phải tiếp tục chạy (thực thi, test, vòng lặp debug) cho đến khi hoàn thành triệt để mục tiêu (goal) đã được đề ra từ đầu.
   - Khi cần thiết, người dùng có thể kích hoạt slash command `/goal` để Agent vào trạng thái làm việc xuyên suốt, không ngừng nghỉ cho đến khi có kết quả cuối cùng.

4. **Chain of Thought (Quá trình tự suy):**
   - Before answering or making any decisions, every Agent MUST write its internal reasoning, step-by-step logic, and self-reflection inside `<thought> ... </thought>` XML tags.
   - The final response should be placed outside and after these tags.
